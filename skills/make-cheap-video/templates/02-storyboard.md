# Cheap Video Storyboard

Compress the narrative into two Veo Lite segments. Each segment must stand on
its own because Veo Lite does not guarantee continuity across separate calls.

## Global Style Lock

`<visual style, color palette, medium, lighting, texture, aspect, no text>`

## Character and World Lock

- Protagonist:
  `<appearance, silhouette, prop, emotional posture>`
- Opposing force:
  `<appearance, scale, motion language>`
- Environment:
  `<location, spatial anchors, what never changes>`
- Audio world:
  `<diegetic ambience and key sound effects only>`

## Segment A: 0-8s, Setup -> Turn

1. `0-2s`:
   `<establish protagonist and emotional state>`
2. `2-5s`:
   `<camera move / pressure enters / first visible contradiction>`
3. `5-8s`:
   `<turn: the world changes, but do not resolve it yet>`

## Segment B: 8-15s, Reveal -> Button

1. `8-10s`:
   `<restate where we are; reveal consequence of segment A>`
2. `10-13s`:
   `<opposing force fills frame / action or transformation>`
3. `13-15s`:
   `<button: one held emotional image, not a new plot point>`

## Continuity Risks

- `<risk>` -> `<how the segment prompts restate or avoid it>`
- `<risk>` -> `<how the segment prompts restate or avoid it>`
